package ui.tabs;

import javax.swing.*;
import java.awt.*;

public class F5ReportsPanel extends JPanel {

    public F5ReportsPanel() {
        setLayout(new BorderLayout());

        JLabel label = new JLabel("F1 - Tồn đầu", SwingConstants.CENTER);
        add(label, BorderLayout.CENTER);
    }
}